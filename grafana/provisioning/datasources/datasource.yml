apiVersion: 1

datasources:
  # --- PROMETHEUS --- #
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  # --- LOKI --- #
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100